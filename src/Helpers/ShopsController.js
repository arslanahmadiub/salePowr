
export default class Shops {


    static addNew(shopData, user) {
        /** make you api call HERE then return to the caller
            true if it succeeded, false otherwise
        */

        return true;
    }

    static updateShop(shopId, shopData, user) {
        /** make you api call HERE then return to the caller
            true if it succeeded, false otherwise
        */

        return true;

    }
}